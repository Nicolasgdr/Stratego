#ifndef COLOR_H
#define COLOR_H

/**
 * @brief The colors of both players in the game.
 */
enum Color
{
    RED,        /**< color representing the red player. */
    BLUE,       /**< color representing the blue player. */

    COLOR_NUMBER, /**< the number of color in the game. */

    NONE        /**< default color without associted player. */
};

#endif // COLOR_H
